{% extends 'base.html.twig' %}

{% block title %}LangHasUser index{% endblock %}

{% block body %}
    <h1>LangHasUser index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lang_code</th>
                <th>User_id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lang_has_user in lang_has_users %}
            <tr>
                <td>{{ lang_has_user.id }}</td>
                <td>{{ lang_has_user.langCode }}</td>
                <td>{{ lang_has_user.userId }}</td>
                <td>

                    <a href="{{ path('app_lang_has_user_edit', {'id': lang_has_user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lang_has_user_new') }}">Create new</a><br>
    <a href="{{ path('app_user_index') }}">back to user</a>
{% endblock %}
